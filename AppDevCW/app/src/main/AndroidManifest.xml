<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scott.appdevcw">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fitness Buddy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainmenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".displaycalorie" />
        <activity android:name=".bmi" />
        <activity android:name=".workout" />
        <activity android:name=".calculatecalories" />
        <activity android:name=".workoutselector" />
        <activity android:name=".macroscalc" />
        <activity android:name=".savedinfo" />
        <activity android:name=".test" />
        <activity android:name=".informationtext" />
    </application>

</manifest>